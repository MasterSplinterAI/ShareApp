# ============================================
# Backend .env for Production Server
# ============================================
# Copy this to backend/.env on the server and update values

# Daily.co Configuration
DAILY_API_KEY=your_daily_api_key_here

# Server Configuration
# Backend runs on port 3000 (proxied through Nginx)
PORT=3000

# Frontend URL (for CORS) - Production domain
FRONTEND_URL=https://share.jarmetals.com
