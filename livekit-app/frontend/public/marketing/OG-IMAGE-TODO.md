# OG image placeholder

`index.html` currently points `og:image` at `/marketing/hero-kenny.jpg` as an interim
social-share image.

TODO: design a branded 1200x630 `og-card.png` (Lalia logo + tagline), place it in
`public/marketing/og-card.png`, and update the `og:image` meta tag in `index.html` to
`https://lalia.cloud/marketing/og-card.png`.
