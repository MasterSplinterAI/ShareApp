# Frontend env (copy to .env.local for Vite)
# Legacy: previously gated "Try V2 workspace" on classic home. Unused — V2 is the default host path.
# VITE_V2_ENTRY_ENABLED=true
# Optional API base (default: same-origin /api)
# VITE_API_URL=/api
