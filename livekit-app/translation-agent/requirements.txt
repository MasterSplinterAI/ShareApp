# NOTE: The deployed agent image installs from requirements_livekit.txt
# (see Dockerfile: COPY requirements_livekit.txt requirements.txt).
# This file exists only so local tooling resolves the same dependency set.
# Do NOT add packages here — edit requirements_livekit.txt instead.
-r requirements_livekit.txt
