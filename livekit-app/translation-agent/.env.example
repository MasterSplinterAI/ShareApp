# Translation Agent — environment variables
# Copy to .env and fill in real values.

# LiveKit credentials
LIVEKIT_URL=wss://your-server.livekit.cloud
LIVEKIT_API_KEY=your-api-key
LIVEKIT_API_SECRET=your-api-secret
AGENT_NAME=translation-cloud-prod

# STT provider: deepgram (primary) | openai (emergency fallback)
STT_PROVIDER=deepgram
DEEPGRAM_API_KEY=
OPENAI_API_KEY=

# LLM provider for translation: openai (gpt-4o-mini)
LLM_PROVIDER=openai

# Cost event reporting
# Must match COST_EVENT_SECRET in backend/.env
BACKEND_BASE_URL=http://localhost:3001
COST_EVENT_SECRET=replace-with-32-byte-hex

# Optional tuning
VAD_ACTIVATION_THRESHOLD=0.4
DEEPGRAM_ENDPOINTING_MS=400
LIVEKIT_CLOUD=false
